// Create global
BasUtils = {};

// Create client
BasUtils.client = {};

// Create server
BasUtils.server = {};

// Create config
BasUtils.config = {};

// Set global config
BasUtils.config = {
    version: '0.1.0'
};
